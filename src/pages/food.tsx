import Container from 'components/Container';

export const Food = () => {
    const a = ``;
    console.log(a);

    return (
        <>
            <Container>
                Food
            </Container>
        </>
    );
};
