import Container from 'components/Container';

export const Events = () => {
    const a = ``;
    console.log(a);

    return (
        <>
            <Container>
                Events
            </Container>
        </>
    );
};
